services:
  # MongoDB Database (Replica Set for Transaction Support)
  mongo:
    image: mongo:6
    container_name: build-risk-mongo
    command: ["--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: >
        mongosh --quiet --eval "
          try { 
            rs.status().ok 
          } catch(e) { 
            rs.initiate({_id: 'rs0', members: [{_id: 0, host: 'localhost:27017'}]}); 
            1 
          }"
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 30s

  # RabbitMQ Message Broker
  rabbitmq:
    image: rabbitmq:3.12-management
    container_name: build-risk-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: myuser
      RABBITMQ_DEFAULT_PASS: mypass

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: build-risk-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      start_period: 20s
      retries: 5

  # SonarQube Server
  sonarqube:
    image: sonarqube:latest
    container_name: build-risk-sonarqube
    environment:
      SONAR_JDBC_URL: jdbc:postgresql://sonar-db:5432/sonar
      SONAR_JDBC_USERNAME: sonar
      SONAR_JDBC_PASSWORD: sonar
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
      # Allow more concurrent connections for parallel scanner submissions
      SONAR_WEB_JAVAADDITIONALOPTS: "-Dsonar.web.http.maxThreads=100"
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs
    ports:
      - "9000:9000"
    depends_on:
      - sonar-db
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9000/api/system/status || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s

  # PostgreSQL Database for SonarQube
  sonar-db:
    image: postgres:15-alpine
    container_name: build-risk-sonar-db
    environment:
      POSTGRES_USER: sonar
      POSTGRES_PASSWORD: sonar
      POSTGRES_DB: sonar
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  # Trivy Vulnerability Scanner Server
  # NOTE: First run downloads ~600MB vulnerability DB (cached in volume)
  trivy:
    image: aquasec/trivy:latest
    container_name: build-risk-trivy
    command: server --listen 0.0.0.0:4954 --cache-dir /root/.cache/trivy
    environment:
      # Set GitHub token to avoid rate limiting when fetching GHSA advisories
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    ports:
      - "4954:4954"
    volumes:
      - trivy_cache:/root/.cache/trivy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:4954/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Celery Worker for Data Processing
  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: build-risk-celery-worker
    command: uv run celery -A app.celery_app worker -Q pipeline.default,ingestion,processing,validation,sonar_scan,trivy_scan --loglevel=info -c 4
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    restart: unless-stopped
    environment:
      MONGODB_URI: mongodb://mongo:27017
      MONGODB_DB_NAME: buildguard
      CELERY_BROKER_URL: amqp://myuser:mypass@rabbitmq:5672//
      REDIS_URL: redis://redis:6379/0
      HOME: /tmp  # Required for git operations
      # Security
      SECRET_KEY: ${SECRET_KEY:-your-secret-key-change-in-production}
      # SonarQube Scanner JVM options
      SONAR_SCANNER_OPTS: "-Xmx2g -Xms512m"
      # SonarQube settings
      SONAR_HOST_URL: http://sonarqube:9000
      SONAR_TOKEN: ${SONAR_TOKEN:-}
      SONAR_DEFAULT_PROJECT_KEY: build-risk-ui
      SONAR_WEBHOOK_SECRET: ${SONAR_WEBHOOK_SECRET:-change-me}
      # Trivy settings
      TRIVY_SERVER_URL: http://trivy:4954
      # GitHub settings
      GITHUB_APP_ID: ${GITHUB_APP_ID:-}
      GITHUB_APP_PRIVATE_KEY: ${GITHUB_APP_PRIVATE_KEY:-}
      GITHUB_INSTALLATION_ID: ${GITHUB_INSTALLATION_ID:-}
    volumes:
      - ./backend:/app
      - shared_data:/app/data
    depends_on:
      mongo:
        condition: service_healthy
      rabbitmq:
        condition: service_started
      redis:
        condition: service_healthy
      sonarqube:
        condition: service_started

  # Celery Beat Scheduler
  celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: build-risk-celery-beat
    command: uv run celery -A app.celery_app beat --loglevel=info --schedule=/app/data/celerybeat-schedule
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    restart: unless-stopped
    environment:
      MONGODB_URI: mongodb://mongo:27017
      MONGODB_DB_NAME: buildguard
      CELERY_BROKER_URL: amqp://myuser:mypass@rabbitmq:5672//
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY: ${SECRET_KEY:-your-secret-key-change-in-production}
      HOME: /tmp
    volumes:
      - ./backend:/app
      - shared_data:/app/data
    depends_on:
      mongo:
        condition: service_healthy
      rabbitmq:
        condition: service_started

  # Backend API Server
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: build-risk-backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    restart: unless-stopped
    environment:
      MONGODB_URI: mongodb://mongo:27017
      MONGODB_DB_NAME: buildguard
      CELERY_BROKER_URL: amqp://myuser:mypass@rabbitmq:5672//
      REDIS_URL: redis://redis:6379/0
      DEBUG: "True"
      # SonarQube settings
      SONAR_HOST_URL: http://sonarqube:9000
      SONAR_TOKEN: ${SONAR_TOKEN:-}
      SONAR_DEFAULT_PROJECT_KEY: build-risk-ui
      SONAR_WEBHOOK_SECRET: ${SONAR_WEBHOOK_SECRET:-change-me}
      SONAR_WEBHOOK_PUBLIC_URL: ${SONAR_WEBHOOK_PUBLIC_URL:-http://localhost:8000/api/sonar/webhook}
      # GitHub settings
      GITHUB_APP_ID: ${GITHUB_APP_ID:-}
      GITHUB_APP_PRIVATE_KEY: ${GITHUB_APP_PRIVATE_KEY:-}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID:-}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET:-}
      GITHUB_WEBHOOK_SECRET: ${GITHUB_WEBHOOK_SECRET:-}
      SECRET_KEY: ${SECRET_KEY:-your-secret-key-change-in-production}
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - shared_data:/app/data
    depends_on:
      mongo:
        condition: service_healthy
      rabbitmq:
        condition: service_started
      redis:
        condition: service_healthy

  # Frontend Application
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: build-risk-frontend
    command: npm run dev
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000/api
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend

  # Monitoring Stack
  # loki:
  #   image: grafana/loki:3.0.0
  #   container_name: build-risk-loki
  #   ports:
  #     - "3100:3100"
  #   command: -config.file=/etc/loki/local-config.yaml
  #   volumes:
  #     - loki_data:/loki

  # alloy:
  #   image: grafana/alloy:latest
  #   container_name: build-risk-alloy
  #   volumes:
  #     - ./monitoring/alloy-config.alloy:/etc/alloy/config.alloy:ro
  #     - /var/run/docker.sock:/var/run/docker.sock:ro
  #     - alloy_data:/var/lib/alloy/data
  #   command: run --server.http.listen-addr=0.0.0.0:12345 --storage.path=/var/lib/alloy/data /etc/alloy/config.alloy
  #   ports:
  #     - "12345:12345"
  #   depends_on:
  #     - loki

  # grafana:
  #   image: grafana/grafana:10.2.3
  #   container_name: build-risk-grafana
  #   restart: unless-stopped
  #   ports:
  #     - "3001:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_USER=admin
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   volumes:
  #     - grafana_data:/var/lib/grafana
  #   depends_on:
  #     - loki

volumes:
  mongo_data:
  postgres_data:
  sonarqube_data:
  sonarqube_extensions:
  sonarqube_logs:
  trivy_cache:  # Trivy vulnerability database cache
  shared_data:  # Shared volume for repo mirrors
  loki_data:
  grafana_data:
  alloy_data:
