# =============================================================================
# GIT HISTORY RECONSTRUCTOR - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy file này thành .env và điền các giá trị thực tế
# Command: cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# GITHUB AUTHENTICATION
# -----------------------------------------------------------------------------
# Personal Access Token của GitHub với quyền repo
# Tạo tại: https://github.com/settings/tokens
# Cần các quyền: repo, workflow
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Tên đầy đủ của repository đích (owner/repo-name)
# Ví dụ: hung-org-117/test-3
REPO_FULL_NAME=your-org/your-repo

# -----------------------------------------------------------------------------
# REPOSITORY PATHS
# -----------------------------------------------------------------------------
# Đường dẫn tuyệt đối đến repository nguồn (chứa lịch sử gốc)
# Đây là repo bạn muốn tái tạo lịch sử từ đó
SOURCE_REPO_PATH=/path/to/source/repo

# Đường dẫn tuyệt đối đến repository đích (repo mới, thường là empty)
# Đây là repo sẽ nhận lịch sử được tái tạo
TARGET_REPO_PATH=/path/to/target/repo

# -----------------------------------------------------------------------------
# OPTIONAL SETTINGS
# -----------------------------------------------------------------------------
# Số lượng commit cần tái tạo (từ mới nhất)
# Để trống hoặc 0 để tái tạo tất cả commits
# Ví dụ: 100 = chỉ tái tạo 100 commit gần nhất
LIMIT_COMMITS=100

# Thời gian chờ giữa các lần kiểm tra GitHub Actions (giây)
# Mặc định: 30 giây
POLL_INTERVAL=30

# Thời gian timeout tối đa cho mỗi workflow (giây)
# Nếu workflow chạy quá thời gian này, script sẽ tiếp tục với commit tiếp theo
# Mặc định: 600 giây (10 phút)
WORKFLOW_TIMEOUT=600
